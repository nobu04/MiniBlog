     <div class="container">
       <div class="row">
 <div class="col s12 m8">
      <div class="card blue accent-1">
        <div class="card-content black-text">
          <%= article.text %>
        </div>
        <div class="card-action">
          <%  if user_signed_in? && current_user.id = article.user_id %>
          <%= link_to 'edit', "/articles/#{article.id}/edit", method: :get , class: "card-action-color"%>
          <%= link_to 'delete', "/articles/#{article.id}", method: :delete , class: "card-action-color"%>
          <% end %>
        </div>
      </div>
    </div>
        </div>
                </div>


